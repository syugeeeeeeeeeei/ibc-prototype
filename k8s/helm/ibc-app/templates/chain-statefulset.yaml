{{- /* values.yaml の chains リストをループ処理 */}}
{{- range .Values.chains }}
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  # 各チェーンの名前に基づいてStatefulSet名を設定 (例: ibc-app-data-9)
  name: {{ include "ibc-app.fullname" $ }}-{{ .name }}
spec:
  replicas: 1
  serviceName: {{ include "ibc-app.fullname" $ }}-{{ .name }}-headless
  selector:
    matchLabels:
      {{- include "ibc-app.selectorLabels" $ | nindent 6 }}
      app.kubernetes.io/component: chain
      app.kubernetes.io/instance: {{ .name }}
  template:
    metadata:
      labels:
        {{- include "ibc-app.selectorLabels" $ | nindent 8 }}
        app.kubernetes.io/component: chain
        app.kubernetes.io/instance: {{ .name }}
    spec:
      containers:
        - name: chain
          {{- /* .type (datachain/metachain) に応じてイメージを動的に選択 */}}
          {{- $chainType := index $.Values.chainTypes .type }}
          image: "{{ $chainType.repository }}:{{ $chainType.tag }}"
          imagePullPolicy: Never
          command: ["/bin/sh", "-c", "/scripts/entrypoint-chain.sh"]
          # --- 修正箇所 開始 ---
          env:
            - name: CHAIN_APP_NAME
              value: {{ .type | quote }} # "datachain" や "metachain" を環境変数として設定
          # --- 修正箇所 終了 ---
          ports:
            - containerPort: 26657
              name: rpc
            - containerPort: 9090
              name: grpc
            - containerPort: 1317
              name: api
          volumeMounts:
            - name: data
              # --- 修正箇所 開始 ---
              mountPath: /home/{{ .type }}/.{{ .type }}
              # --- 修正箇所 終了 ---
            - name: scripts
              mountPath: /scripts
            - name: mnemonics
              # --- 修正箇所 開始 ---
              mountPath: /etc/mnemonic/key.mnemonic # パスを汎用化
              # --- 修正箇所 終了 ---
              subPath: {{ .name }}
      volumes:
        - name: scripts
          configMap:
            name: {{ include "ibc-app.fullname" $ }}-scripts
            defaultMode: 0755
        - name: mnemonics
          secret:
            secretName: {{ include "ibc-app.fullname" $ }}-mnemonics
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: [ "ReadWriteOnce" ]
        resources:
          requests:
            storage: {{ $.Values.chain.storage.size }}
{{- end }}